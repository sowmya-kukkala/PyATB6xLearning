# from dir import *  # Can't be used to import

from package import util_module # If we want to import from one location to another then define the package else directory

import mymodule
import os
import math
import re


print(mymodule.greet("Promo")) # Hello Promo